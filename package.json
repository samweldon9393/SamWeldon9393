{
  "name": "sam-weldon-website",
  "version": "1.0.0",
  "private": true,
  "description": "Personal portfolio site for sam-weldon.com",
  "scripts": {
    "build": "tailwindcss -i ./src/styles.css -o ./pub/styles.css",
    "watch": "tailwindcss -i ./src/styles.css -o ./pub/styles.css --watch",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "tailwindcss": "3.4.15"
  }
}
